<%= link_to schedule.name, cohort_schedule_path(schedule.cohort, schedule), class: "btn btn-info btn-xs" %>
<%= link_to 'x', cohort_schedule_path(schedule.cohort, schedule), method: 'delete', data: {confirm: "Are you sure you want to delete this schedule?"}, class: "btn btn-danger btn-xs" %>
