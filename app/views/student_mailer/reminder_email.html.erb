<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h4>
      Hi, <%= @student.full_name_or_github_name %>! Don't forget you're presenting a blog post tomorrow.
    </h4>

    <br>

    Your blog posts so far:
    <ul>
      <% @student.blog_assignments.each do |blog_assignment| %>
        <% posts_since_previous = @student.blog_entries_written_since_previous_assignment(blog_assignment.due_date) %>
      <li>
        <% if blog_assignment.due_date <= Date.today %>
          <% if posts_since_previous.any? %>
            <%= image_tag attachments[0].url %>
          <% else %>
            <%= image_tag attachments[1].url %>
          <% end %>
        <% else %>
          <%= image_tag attachments[2].url %>
        <% end %> 
        <%= blog_assignment.due_date.strftime("%A, %m/%d") %>
      </li>
      <% end %>
    </ul>
    <br>
    Happy Blogging!
    <br>
    <br>
    // &lt;3,
    <br>
    <br>
    Your Flatiron School Instructors
  </body>
</html>
